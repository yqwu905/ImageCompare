# ImageComparator 架构与开发指南

本文档为项目的技术架构、编程规范及开发路线图 (ROADMAP) 的详细说明。

## 1. 细化的技术栈
为了满足高性能、现代化的要求，本项目采用以下核心技术与工具：

- **编程语言**: C++ (C++17 或更高标准)
- **UI 框架**: Qt 6 (使用 QML / Qt Quick)
  - **QML**: 用于构建现代化、硬件加速的流畅界面。
  - **C++**: 处理底层逻辑、图像加载优化（多线程预加载、缓存）、核心文件系统访问及比较逻辑。
- **图像支持**: 原生支持 JPG, PNG。通过 Qt Image Formats 模块 (`qtimageformats`) 支持 WEBP 和 TIFF。
- **构建系统**: CMake
- **编译器**: MSVC (Microsoft Visual C++)，面向 Windows 平台优化。
- **自动化与部署**: GitHub Actions
  - 使用 `windeployqt` 进行依赖收集与分发打包。

## 2. 编程规范
代码质量对于本项目的长远发展至关重要。

### 2.1 格式与静态检查
- **Clang-Format**: 所有的 C++ 代码必须通过 `clang-format` 格式化（参考项目根目录的 `.clang-format` 配置文件）。
- **Clang-Tidy**: 所有的代码提交都将通过 `clang-tidy` 进行静态分析（参考项目根目录的 `.clang-tidy` 配置文件），以发现潜在的性能瓶颈、内存泄漏以及代码逻辑问题。
- **CI 强制拦截**: 任何不满足格式化要求或存在 `clang-tidy` 警告的提交都将在 GitHub Actions 校验流程中失败。

### 2.2 单元测试与 TDD
- **测试框架**: 采用 **QTest** 框架进行 C++ 和部分 Qt 逻辑的单元测试。
- **功能覆盖**: 每个功能点（例如：文件路径解析、匹配算法、多线程加载管理器、标注逻辑）在开发完成后，都**必须**编写能够覆盖实际使用场景和边界条件的单元测试。
- **CI 执行**: 测试将被集成到 CMake 的 CTest 中，每次推送 (Push) 和合并请求 (PR) 都会在 CI 中自动执行所有的测试用例。

## 3. 开发 ROADMAP

### Phase 1: 项目基础设施搭建 (Current)
- [x] 初始化 CMake C++/Qt6 项目。
- [x] 配置 `clang-format` 与 `clang-tidy`。
- [x] 集成 QTest 单元测试框架。
- [x] 配置 GitHub Actions CI（格式检查、静态检查、编译、测试）。
- [x] 配置 GitHub Actions CD（利用 MSVC 和 windeployqt 构建和发布 Release 压缩包）。

### Phase 2: 核心底层逻辑与测试
- [ ] **图像加载与缓存管理器**: 开发高性能的图片异步加载模块，支持预加载与 LRU 内存缓存。确保 WEBP, TIFF 等格式正确加载。
- [ ] **文件夹解析与匹配引擎**: 实现读取多个文件夹内图片逻辑。
  - 实现“按顺序匹配”算法。
  - 实现“按文件名匹配”算法。
- [ ] **单元测试**: 为缓存管理器、文件解析器和匹配引擎编写全面的 QTest 用例。

### Phase 3: QML 前端界面与基础交互
- [ ] **主框架设计**: 搭建基于 QML 的现代化 UI 主体框架（菜单栏、侧边栏、主工作区）。
- [ ] **文件夹导入模块**: 提供用户界面以选择并导入多组图片文件夹。
- [ ] **C++/QML 通信**: 将底层的匹配引擎及图像数据模型 (QAbstractListModel) 暴露给 QML 前端。

### Phase 4: 图像对比与标注功能
- [ ] **对比视图实现**: 在 QML 中实现平滑的图像网格、并排对比视图，支持同步缩放与拖拽。
- [ ] **硬件加速优化**: 确保大图片切换及渲染的性能，利用 QSG (Qt Scene Graph) 进行优化。
- [ ] **图片标注功能**: 支持在界面上对每张图片悬浮显示或覆盖绘制自定义标注文本（标明图片来源批次）。

### Phase 5: 性能调优与最终发布
- [ ] **性能分析**: 使用工具进行性能打点，优化图像加载延迟，减少内存占用。
- [ ] **用户体验提升**: 增加动画过渡效果、快捷键支持和主题调整（如深色模式）。
- [ ] **V1.0 发布**: 验证打包流程，通过 GitHub Actions 自动发布 v1.0.0 的 Windows 绿色免安装版 (zip)。